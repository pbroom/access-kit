{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://access-kit.example/schemas/audit-export.schema.json",
  "title": "AuditEventExport",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "exportId",
    "generatedAt",
    "periodStart",
    "periodEnd",
    "format",
    "target",
    "schemaVersion",
    "includesPayloadHashes",
    "exportedEventCount",
    "sourceEventIds",
    "records",
    "auditIntegrity",
    "version"
  ],
  "properties": {
    "exportId": { "type": "string", "pattern": "^[a-z0-9_:-]+$" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "periodStart": { "type": "string", "format": "date-time" },
    "periodEnd": { "type": "string", "format": "date-time" },
    "format": { "type": "string", "const": "jsonl" },
    "target": { "type": "string", "enum": ["operator_download", "siem_forwarder"] },
    "schemaVersion": { "type": "string", "const": "audit-event:v1" },
    "includesPayloadHashes": { "type": "boolean", "const": true },
    "exportedEventCount": { "type": "integer", "minimum": 0 },
    "sourceEventIds": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9_:-]+$" }
    },
    "records": {
      "type": "array",
      "items": { "type": "string", "minLength": 2 }
    },
    "auditIntegrity": { "$ref": "#/$defs/auditIntegrityReport" },
    "version": { "type": "string", "const": "audit-event-export:v1" }
  },
  "$defs": {
    "auditIntegrityReport": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "eventCount", "verifiedAt", "findings", "version"],
      "properties": {
        "status": { "type": "string", "enum": ["verified", "failed"] },
        "eventCount": { "type": "integer", "minimum": 0 },
        "verifiedAt": { "type": "string", "format": "date-time" },
        "firstEventId": { "type": "string", "pattern": "^[a-z0-9_:-]+$" },
        "lastEventId": { "type": "string", "pattern": "^[a-z0-9_:-]+$" },
        "firstEventHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "lastEventHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "findings": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["code", "message", "severity"],
            "properties": {
              "code": { "type": "string", "minLength": 1 },
              "message": { "type": "string", "minLength": 1 },
              "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
              "eventId": { "type": "string", "pattern": "^[a-z0-9_:-]+$" },
              "expected": { "type": "string", "minLength": 1 },
              "actual": { "type": "string", "minLength": 1 }
            }
          }
        },
        "auditEventId": { "type": "string", "pattern": "^[a-z0-9_:-]+$" },
        "version": { "type": "string", "minLength": 1 }
      }
    }
  }
}
